{
  "name": "wanderview",
  "version": "1.0.0",
  "description": "First-person walking game exploring Hell's Kitchen NYC with AI narration",
  "scripts": {
    "dev": "cd game-ui && npm run dev",
    "build": "cd game-ui && npm install && npm run build",
    "preview": "cd game-ui && npm run preview",
    "postinstall": "cd game-ui && npm install"
  }
}
